Audit the Q3/Q4 prevention actions (FRESH eyes — do NOT use RC audit reasoning). Output weaknesses[] and a verdict.

FIRST, step back to the framing level before checking the gate tests:
- Do these preventions actually address the ROOT CAUSE, or are they band-aids on a symptom?
- Could we be preventing the wrong thing because the problem was misframed?
- Are they at the right altitude (systemic/process control), not a disguised one-off corrective fix?

For each weakness: quadrant (q3_mrc_nc|q4_mrc_nd), classification (ADDRESSABLE|RESIDUAL|OVER_SCOPED), issue, suggested_fix, evidence (URL if WebSearch used).

Checks:
1. Gate test all 4 PASS? (scope, persistence, measurability, proportionality)
2. Hierarchy level 1-3? Or corrective-in-disguise?
3. Failure mode of prevention named and plausible?
4. PROPORTIONALITY — is the control over-engineered for the incident? A standing ritual, scheduled review, periodic audit, "check every N months", or a new standing role/owner where a one-line structural gate (a CI assertion, a schema constraint, a detect-at-creation check) would do is OVER_SCOPED. Such a control is Rung-4/5 administrative and compliance-dependent ("nobody actually does it"). When you find this, emit a weakness with classification OVER_SCOPED AND a proportionate suggested_fix (the smaller, higher-on-the-hierarchy structural control that should REPLACE it). The pipeline will CUT the bloated control down to your suggested_fix, so make the suggested_fix a complete drop-in replacement control.
5. When uncertain about stronger state-of-the-art prevention, invoke WebSearch to benchmark — but use it at most TWICE, only for the single most decisive claim. Do not exhaustively search; rely mainly on the provided research and your reasoning.

verdict:
- REWORK = the prevention actions have a framing-level/structural flaw (preventing the wrong thing, band-aid on a symptom, wrong altitude) that requires REGENERATING the actions, not just annotating them.
- OVER_SCOPED = at least one prevention is over-engineered (a standing ritual / periodic audit / "check every N months" where a structural one-line gate suffices); it must be CUT down to a proportionate control (supply the OVER_SCOPED weakness + suggested_fix replacement).
- EXHAUSTED = sound and only RESIDUAL weaknesses remain.
- CONTINUE = sound but one more in-place annotation round would still find ADDRESSABLE issues.

## Active Identity & Rename Rule (MANDATORY)

Active skill identity: `skill-ai-escape-mrc` / `ai_escape_mrc` / `run_ai_escape_mrc.py` / `trigger_ai_escape_mrc.py`. When you coin or quote command names, paths, hooks, CLIs, env vars, or report directories in `issue`, `suggested_fix`, or any `evidence`/`rationale` field, you MUST use the active identity. Do not emit any of these legacy literals:

| Forbidden legacy literal | Rewrite as |
|--------------------------|------------|
| `eightd-<name>` (any binary/CLI) | `aem-<name>` |
| `skill-8d-mrc` | `skill-ai-escape-mrc` |
| `run_8d` | `run_ai_escape_mrc` |
| `trigger_8d` | `trigger_ai_escape_mrc` |
| `8d-reports` (directory name) | `ai-escape-mrc-reports` |
| `pending-8d` (file/dir prefix) | `pending-ai-escape-mrc` |
| `CLAUDE_EIGHTD` (env var prefix) | `CLAUDE_AI_ESCAPE_MRC` |

Output containing any forbidden literal will be rejected by the validator and you will be asked to regenerate this audit response.
