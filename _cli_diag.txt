Now I have the full picture. Here's the answer:

---

## 三週前沒遷移的原因：問題還沒惡化到觸發點

時間線很清楚：

### 三週前（~4/1）根本還沒有壓力

- 4/9 才開始出現密集的 daily_brief 問題：MSAL token fallback、port 搶佔、JSON parse 崩壞、Copilot 自動重啟
- 4/10 加入 Claude CLI 作為 API fallback（OpenRouter 額度用完的應急方案）—— 這時候在補洞，還沒到質疑架構的程度

### 4/16–4/17 是轉折期：8D 大量爆發

- 4/16 一天之內：CDP port 被搶、Copilot fallback 邏輯錯誤、cache mismatch、MSAL scope 對不上 —— 開始用 8D MRC 做系統性分析
- 4/17 產出 **8 個 8D 報告**（P1–P8），加了 Stop hook Gate 3、pre-generation gate、pipeline smoke tests
- 這時候的策略還是「再多加 hook」—— 因為 hook 的 84% ceiling 還沒被量化證明

### 4/20 是斷裂點：單日 5 次同類復發

關鍵 commit `dd7aa32` 記錄得很清楚：

> **同一天 5 次 hook governance 復發**。Hook 機械上有 fire（MANDATORY text 有注入到 Claude），但 Claude 把 "research existing solutions" 解釋成「在 Claude Code 內搜尋方案」，而不是「這個框架本身對不對」。

這天產出了 **migration decision framework**，用客觀標準證明了：

1. Hook governance 社群基準 ~84% compliance，天花板是結構性的（LLM 是 decision-maker，hook 只是 observer）
2. 語義層面的 research breadth 用 grep 查不出來（hook 只能查「有沒有搜」，不能查「搜對了沒」）
3. **Migration trigger 條件 #1 已滿足**：同一 failure class 復發 ≥5 次

### 所以答案是：

**三週前（~4/1）條件不成立。** Migration trigger 需要的「同一 failure class 復發 ≥5 次 despite hook governance」在 4/20 才首次達標。之前的問題是散發性的（不同 root cause），而且 hook 還在有效覆蓋範圍內。

時間軸：
- **4/1–4/8**：穩定運作期，沒有遷移的理由
- **4/9–4/15**：問題開始密集出現，但還在 fix-per-bug 模式
- **4/16–4/17**：8D 分析大量產出，開始加 hook gates
- **4/20**：Hook 天花板被正式量化 → migration decision → spec 在同日完成
- **4/21（今天）**：LangGraph FSM 完整實作（30+ commits in one day）

本質上就是 wiki 裡記錄的 **[Instruction Failure Escalation Protocol](concepts/wiki-to-code-traceability.md)** 在實際發生：text instruction → soft gate (hook) → 發現 ceiling → architectural elimination (LangGraph FSM)。三週前還卡在第一層，沒有足夠的失敗數據來證明需要跳到最後一層。
